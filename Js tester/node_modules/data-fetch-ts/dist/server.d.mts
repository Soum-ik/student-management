type Methods = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';
type cache = 'default' | `force-cache` | `no-cache` | `no-store ` | `only-if-cached` | `reload`;
type RequestBody = {
    [key: string]: any;
};
type FetchOptions<M extends Methods> = M extends 'POST' | 'PUT' | 'PATCH' ? {
    endpoint: string;
    method: M;
    token?: string;
    body: RequestBody;
    cash?: cache;
} : {
    endpoint: string;
    method?: M;
    token?: string;
    cash?: cache;
    body?: RequestBody;
};
declare const fetchData: <M extends Methods>({ endpoint, method, token, body }: FetchOptions<M>) => Promise<any>;

export { fetchData as default };
